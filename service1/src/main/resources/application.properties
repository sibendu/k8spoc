spring.data.mongodb.host=${DB_HOST}
spring.data.mongodb.port=${DB_PORT}
spring.data.mongodb.database=${DB_NAME}
#spring.data.mongodb.username=adminuser
#spring.data.mongodb.password=password123

aws.access.key=${AWS_ACCESS_KEY} 
aws.secret.key=${AWS_SECRET}

aws.kinesis.stream=${KINESIS_STREAM_NAME}
aws.kinesis.partitionKey=${KINESIS_PARTITION_KEY}

